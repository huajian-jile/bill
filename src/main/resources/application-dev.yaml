spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bill
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: public
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql